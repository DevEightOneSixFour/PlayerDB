<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/iv_game_avatar"
        android:layout_margin="16dp"
        android:layout_width="250dp"
        android:layout_height="250dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/tv_game_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Minecraft Player"
        android:textSize="24sp"
        android:layout_margin="8dp"
        app:layout_constraintTop_toBottomOf="@id/iv_game_avatar"
        app:layout_constraintStart_toStartOf="@id/iv_game_avatar"
        app:layout_constraintEnd_toEndOf="@id/iv_game_avatar"/>

</androidx.constraintlayout.widget.ConstraintLayout>